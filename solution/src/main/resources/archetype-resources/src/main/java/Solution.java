package ${package};

import java.util.*;

public class Solution {

    public ${returnType} ${methodName}(${argumentType} ${argumentName}) {
        ${returnType} result = :;

        return result;
    }

}
